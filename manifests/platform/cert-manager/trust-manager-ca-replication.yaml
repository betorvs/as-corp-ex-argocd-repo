apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: internal-ca-bundle
spec:
  sources:
    # 1. Source your internal root CA certificate
    - secret:
        name: "root-ca-secret"
        key: "tls.crt"
  target:
    configMap:
      key: "ca-bundle.crt"